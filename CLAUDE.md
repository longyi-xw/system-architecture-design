# 系统架构设计师 · 学习笔记仓库

软考高级「系统架构设计师」备考仓库。用户阅读实体书后提供粗浅笔记，由 `study-notes` 技能整理成精简笔记，最终完成全书总结、记忆卡片和复习体系。

## 每次会话必做（按顺序执行）

1. **先读 `PROGRESS.md`**，确认书籍信息、当前进度、下一步动作。进度以该文件为准，不要依赖对话历史。
2. 用 `git status` 检测 `notes/` 下**新增或修改**的笔记文件，它们就是本次要处理的对象；原稿被修改过的，需同步更新对应的总结和记忆卡片。
3. 处理笔记时**必须调用 `study-notes` 技能**，不要徒手总结。分析、纠错和疑问先提给用户审核，**用户确认后**才写入总结文件。
   - 例外（2026-08-07 起，用户明确指示）：处理「网络取材」小节时，可按保守判断（存疑一律 ⚠️ 标注、不确凿的检索结果不采纳）直接撰写并写入总结文件，不必逐条等待确认；但需在完成一批小节后，向用户汇总列出全部存疑点，供用户审核调整，调整后再视情况修改文件。
4. 每完成一个小节/章节的总结，立即依次执行：写入总结文件 → 更新记忆卡片 → 更新 `PROGRESS.md`（进度表 + 会话日志）→ git commit → `git push origin main`。做完才算完成，不要攒到会话结尾。

## 铁律（防幻觉，任何情况下不得违反）

1. 笔记内容只能来自以下来源：① **用户提供的笔记原稿**；② **用户在答疑中确认的结论**；③（2026-08-06 起，经用户授权，见下方「网络取材来源」）**指定的网络笔记仓库与检索到的权威资料**。禁止凭模型自身记忆直接补充书中内容——任何知识点都必须能追溯到某个具体来源（原稿 / 用户结论 / 具体网页链接）。
2. 网络检索资料（含②③）写入总结前必须列出要点和来源，经用户确认才能写入总结文件；无论用于印证、补漏还是作为主线索材料，都适用本条。
3. 不确定、多来源冲突、或找不到出处的内容一律放入「待核对 · 存疑」区并用 ⚠️ 标注，禁止写成定论。
4. 书的章节目录、章节名以 `PROGRESS.md` 记录为准；2026-08-06 起，`PROGRESS.md` 的章节目录以「网络取材来源」中笔记仓库的目录为准（用户已确认）。目录之外的新增/调整、或来源之间目录有出入时，先问用户，禁止猜测或凭记忆填写。
5. 已归档的笔记文件（`summaries/`、`review/` 下）只在两种情况下可修改：对应原稿笔记更新后的同步更新、用户明确要求；修改内容需经用户审核确认。
6. 做不到、找不到、不知道，就直说，禁止编造。

## 网络取材来源（2026-08-06 起生效）

- **起因**：实体书阅读时间有限，用户授权自第 2 章 2.3.5 小节起，改为以网络资料整理笔记，替代逐节手抄笔记原稿的方式。
- **主线索材料**：[lcp0578/book-note](https://github.com/lcp0578/book-note/tree/master/books/ruankao/系统架构设计师教程)（`chapter1.md` ~ `chapter19.md`，与 `PROGRESS.md` 章节目录一一对应）。该仓库目录本身存在个别编号笔误（如「13、4」应为 13.4），整理时按合理编号订正，并在会话中告知用户。
- **补充材料**：检索到的其他权威软考笔记网站，用于交叉印证、补充主线索材料的遗漏；写入总结时随来源列出。
- **适用范围**：第 2 章 2.3.5 小节及之后的全部小节、第 3 章起的后续章节。已完成的第 1 章、第 2 章 2.1–2.3.4 不受影响，除非用户明确要求按新流程重做。
- **notes/ 原稿约定的例外**：网络取材小节没有用户手写原稿，不在 `notes/` 下建立对应文件；`study-notes` 技能直接以网络材料为输入产出 `summaries/` 总结，仍需先给用户看要点与存疑清单、用户确认后才写入。

## 笔记风格（用户明确要求）

- **精简、核心、面向记忆**：只留需要记住的内容，删掉解释性废话、空洞过渡句和显而易见的常识。
- 重要级别三级标记，写在条目开头：
  - 🔴 必背 —— 用户特殊说明「需要重点记忆」的内容（必须同时进入记忆卡片）
  - 🟡 重点 —— 需理解并大概率考察的内容
  - 🟢 了解 —— 知道即可
- 用户没有特殊说明时，由整理者按考试常识初步分级，并在交付时提醒用户可调整。
- 保留用户笔记中有记忆点的原话、类比和例子，不为了「规范」抹平。

## 目录结构与命名（严格遵守，不新增顶层目录）

```
CLAUDE.md                                  本文件：约束与工作流
PROGRESS.md                                唯一进度来源：书籍信息、章节目录、进度表、会话日志
notes/第N章-章名/N.M 小节名.md               用户笔记原稿（用户手写，模型禁止重命名或修改）
summaries/第N章-章名/N.M-小节名.md           小节总结（study-notes 小节模板）
summaries/第N章-章名/第N章-章节总结.md        章节总结（study-notes 章节模板）
summaries/全书总结.md                       全书总结（最终产出）
review/flashcards/第N章-章名.md             每章记忆卡片
review/复习策略.md                          复习方法与间隔复习计划
```

- 章目录名以用户在 `notes/` 下实际创建的为准（如 `第一章-绪论`），`summaries/` 与 `review/flashcards/` 沿用相同章名。
- 更细的子小节笔记（如 1.1.1、1.1.2）归并进所属小节（1.1）的总结文件，不单独建总结文件。
- 章节总结的素材 = `summaries/第N章-章名/` 下已有的各小节总结文件；全书总结的素材 = 各章章节总结文件。**素材文件不齐时不得总结**，先告知用户缺哪些。

## 记忆卡片格式（review/flashcards/第N章-章名.md）

```markdown
### Q: 问题？
- A: 答案（一两句，可背诵）
- 级别: 🔴 ｜ 出处: 1.2 节
```

所有 🔴 条目必须有对应卡片；🟡 酌情；🟢 不做卡片。

## Git 约定

- 提交信息格式：`第N章: 动作描述`，如 `第一章: 完成 1.1 小节总结与记忆卡片`；非笔记类用 `chore: 描述`。
- 一个学习单元（一个小节或一章）一次提交，不要把多章混在一次提交里。
- 远程为 `origin`（GitHub），每个学习单元 commit 后直接 `git push origin main`，不必再逐次征询。
- 只在 `main` 分支上操作，不开分支、不发 PR；除推送本仓库外不做其他外部操作。

## 上下文压缩策略

- 一切状态落盘：进度在 `PROGRESS.md`，内容在笔记文件里。上下文被压缩或开新会话后，读文件即可完全恢复，无需回忆对话。
- 一个小节处理完是天然断点：commit 之后如对话已经很长，主动建议用户 `/compact` 或开新会话，**不要在临近上下文上限时开始新小节**。
- 压缩后继续工作时，重新读 `PROGRESS.md` 和当前小节的相关文件再动手，禁止凭压缩摘要里的印象直接写笔记。
