# 系统架构设计师 · 学习笔记仓库

软考高级「系统架构设计师」备考仓库。用户阅读实体书后提供粗浅笔记，由 `study-notes` 技能整理成精简笔记，最终完成全书总结、记忆卡片和复习体系。

## 每次会话必做（按顺序执行）

1. **先读 `PROGRESS.md`**，确认书籍信息、当前进度、下一步动作。进度以该文件为准，不要依赖对话历史。
2. 用户提供笔记初稿时，**必须调用 `study-notes` 技能**处理，不要徒手总结。
3. 每完成一个小节/章节的总结，立即依次执行：写入对应笔记文件 → 更新记忆卡片 → 更新 `PROGRESS.md`（进度表 + 会话日志）→ git commit。三步做完才算完成，不要攒到会话结尾。

## 铁律（防幻觉，任何情况下不得违反）

1. 笔记内容只能来自两个来源：**用户提供的笔记原稿**、**用户在答疑中确认的结论**。禁止凭模型自身记忆补充书中内容。
2. 网络检索资料只用于印证和提示遗漏，写入笔记前必须经用户确认。
3. 不确定的内容一律放入「待核对 · 存疑」区并用 ⚠️ 标注，禁止写成定论。
4. 书的章节目录、章节名以 `PROGRESS.md` 记录为准；该文件里没有的章节信息，先问用户，禁止猜测或凭记忆填写。
5. 已归档的笔记文件（`summaries/`、`review/` 下）禁止改写或删减，除非用户明确要求修改。
6. 做不到、找不到、不知道，就直说，禁止编造。

## 笔记风格（用户明确要求）

- **精简、核心、面向记忆**：只留需要记住的内容，删掉解释性废话、空洞过渡句和显而易见的常识。
- 重要级别三级标记，写在条目开头：
  - 🔴 必背 —— 用户特殊说明「需要重点记忆」的内容（必须同时进入记忆卡片）
  - 🟡 重点 —— 需理解并大概率考察的内容
  - 🟢 了解 —— 知道即可
- 用户没有特殊说明时，由整理者按考试常识初步分级，并在交付时提醒用户可调整。
- 保留用户笔记中有记忆点的原话、类比和例子，不为了「规范」抹平。

## 目录结构与命名（严格遵守，不新增顶层目录）

```
CLAUDE.md                      本文件：约束与工作流
PROGRESS.md                    唯一进度来源：书籍信息、章节目录、进度表、会话日志
notes/chNN/N.M-raw.md          用户笔记原稿，按小节存档（如 notes/ch01/1.2-raw.md）
summaries/chNN/N.M-小节名.md    小节总结（study-notes 小节模板）
summaries/chNN/chNN-章节总结.md 章节总结（study-notes 章节模板）
summaries/全书总结.md           全书总结（最终产出）
review/flashcards/chNN.md      每章记忆卡片
review/复习策略.md              复习方法与间隔复习计划
```

- 章号两位补零（ch01、ch02…），小节号用「章.节」（1.2、3.4）。
- 章节总结的素材 = `summaries/chNN/` 下已有的各小节总结文件；全书总结的素材 = 各章章节总结文件。**素材文件不齐时不得总结**，先告知用户缺哪些。

## 记忆卡片格式（review/flashcards/chNN.md）

```markdown
### Q: 问题？
- A: 答案（一两句，可背诵）
- 级别: 🔴 ｜ 出处: 1.2 节
```

所有 🔴 条目必须有对应卡片；🟡 酌情；🟢 不做卡片。

## Git 约定

- 提交信息格式：`chNN: 动作描述`，如 `ch01: 新增 1.2 小节总结与记忆卡片`；非笔记类用 `chore: 描述`。
- 一个学习单元（一个小节或一章）一次提交，不要把多章混在一次提交里。
- 只在本仓库内操作，不推送远程（用户未配置远程前）。

## 上下文压缩策略

- 一切状态落盘：进度在 `PROGRESS.md`，内容在笔记文件里。上下文被压缩或开新会话后，读文件即可完全恢复，无需回忆对话。
- 一个小节处理完是天然断点：commit 之后如对话已经很长，主动建议用户 `/compact` 或开新会话，**不要在临近上下文上限时开始新小节**。
- 压缩后继续工作时，重新读 `PROGRESS.md` 和当前小节的相关文件再动手，禁止凭压缩摘要里的印象直接写笔记。
